specVersion: "v1"
serviceId: "admin-portal"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "admin-portal-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
